import { DummyDirectiveDirective } from './dummy-directive.directive';

describe('DummyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DummyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
